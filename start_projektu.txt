mkdir coursehub
cd .\coursehub\
# pip install Django==6.0.7
# python -m django --version
django-admin startproject config . - tworzenie projektu
python .\manage.py runserver
# python .\manage.py runserver 8080
 http://127.0.0.1:8080/ - adres serwera -> http://localhost:8080/
 # python manage.py shell - projekt django z uruchomionym pythonem
 python manage.py migrate - migracja danych do bazy danych
python manage.py test - uruchomienie testow jednostkowych
------
# python .\manage.py createsuperuser - tworzenie admina
Username (leave blank to use 'cscomarch'):
Email address: raj@raj.pll
Password:
Password (again):
This password is too short. It must contain at least 8 characters.
This password is too common.
Bypass password validation and create user anyway? [y/N]: y
Superuser created successfully.
# http://127.0.0.1:8000/admin
--------

-----
tworzenie aplikacji django
# python manage.py startapp courses

----- migracja po dodaniu modeli do nbazy danych

# python .\manage.py makemigrations
# python .\manage.py migrate
# sprawdzenie
 python .\manage.py showmigrations
python .\manage.py sqlmigrate courses 0001
----------

-----
dane w bazie danych
python .\manage.py shell
from courses.models import Trainer, Course
trainer = Trainer.objects.create(first_name="Anna", last_name="Kowalska", email="anna@example.com",)
course = Course.objects.create(title="Django 6 i aplikacje webowe", description="Szkolenie z apliakcji webowych.", price=1890, start_date="2026-09-10", trainer=trainer,)

Course.objects.all()
Course.objects.get(pk=1) # id=1 jeden element zwraca
Course.objects.filter(active=True) # moze zwrócic wiele elementów QuerySet
Course.objects.filter(price__gt=1500)
Course.objects.filter(title__icontains="django")

update:
>>> course=Course.objects.get(pk=1)
>>> course.price = 1990
>>> course.save()

Course.objects.filter(active=False).update(active=True)

delete:
Course.objects.filter(pk=1).delete()